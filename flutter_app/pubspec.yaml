name: energy_monitor_app
description: Real-Time Energy Monitoring
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"  # Unchanged; supports Dart 3.9

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  sqflite: ^2.0.0+4
  fl_chart: ^1.0.0  # Replace charts_flutter
  pdf: ^3.10.8
  printing: ^5.13.2
  path_provider: ^2.0.9
  path: ^1.8.3
  syncfusion_flutter_charts: ^31.2.3  # Fixed: Latest version (2025 stable; resolves cache/directory issues)
  syncfusion_flutter_core: ^31.2.3  # Updated to match
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.2
  rive: ^0.13.20
  syncfusion_flutter_gauges: ^31.2.3  # Updated to match
  lottie: ^3.3.1
  image_picker: ^1.1.2
  provider: ^6.1.2
  permission_handler: ^11.3.1
  riverpod: ^3.0.0
  freezed_annotation: ^2.4.4
  injectable: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  freezed: ^2.4.0
  mockito: ^5.4.0
  json_serializable: ^6.8.0  # Added: Essential for Freezed JSON generation (fixes undefined methods)

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/animations/